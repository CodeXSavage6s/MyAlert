# MyAlert

> ⚠️ **Project Status: Work in Progress** — This library is under active development. Core features are functional but the API may change.

A lightweight, customizable **modal alert library** built with **TypeScript**. MyAlert provides promise-based confirmation dialogs with smooth animations, flexible styling, and zero dependencies.

## ___Built to give you full control___

---

## Features

- 🎯 **Promise-Based API** — Async/await friendly with `boolean` resolution
- 🎨 **Fully Customizable** — Control colors, buttons, text, and animations
- ✨ **Built-in Animations** — `fade-in`, `pop-up`, and `slide-in` out of the box
- 🌙 **Dark Theme Default** — Sleek modern styling ready to use
- ⚡ **Zero Dependencies** — Pure TypeScript, no external libraries
- 📦 **Type Safe** — Full TypeScript support with exported interfaces

---

## Installation

```bash
git clone
https://github.com/CodeXSavage6s/MyAlert.git
```

```bash
cd MyAlert
```

```bash
npm install
```
> ___Note: I plan to make this CDN and later an NPM package___